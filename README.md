# E‑Candle Next.js (TS) — Fixed

Adds the missing devDependencies so Vercel can compile TypeScript:

```json
"devDependencies": {
  "typescript": "5.6.2",
  "@types/react": "18.3.3",
  "@types/node": "20.11.30"
}
```

## Run

```bash
npm install
npm run build
npm start
```
